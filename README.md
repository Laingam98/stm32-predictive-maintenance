# stm32-predictive-maintenance
Real-time vibration analytics for rotating machinery on STM32 MCUs.  DMA → sliding windows → time-domain features (RMS, variance, peaks) → edge anomaly detection. Offline inference, low power, &lt;3 KB RAM footprint.
